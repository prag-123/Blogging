<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('partials/head.ejs') %>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= blog.title %> | Blog Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .blog-card {
            max-width: 720px;
            margin: 0 auto;
            padding: 20px;
        }

        .blog-img {
            max-height: 300px;
            object-fit: cover;
            width: 100%;
            border-radius: 10px;
        }

        .blog-body {
            font-size: 0.95rem;
            line-height: 1.6;
        }

        .back-btn {
            font-size: 0.85rem;
        }
    </style>
</head>

<body>
    <%- include('partials/navbar.ejs') %>

    <div class="container my-4">
        <div class="card shadow-sm rounded-4 blog-card">
            <h2 class="mb-2"><%= blog.title %></h2>
            <p class="text-muted" style="font-size: 0.85rem;">Posted on: <%= blog.createdAt.toDateString() %></p>

            <img src="<%= blog.image %>" alt="Blog Image" class="img-fluid blog-img my-3">

            <p class="blog-body"><%= blog.body %></p>

            <a href="/" class="btn btn-outline-primary mt-3 back-btn">← Back to Home</a>
        </div>

        <hr class="my-4">

        <!-- Comment Section -->
        <div class="mt-4">
            <%- include('comment.ejs') %>
        </div>
    </div>

    <%- include('partials/script.ejs') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
